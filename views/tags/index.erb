<h1>Categories</h1>
  <table class="table">
    <tr>
      <th>Category type</th>
      <th></th>
      <th>Total spent</th>
    </tr>
    <% for tag in @tags %>
    <tr>
      <td><a href="/categories/<%= tag.id %>"><%= tag.type %></a></td>
      <td>|</td>
      <td>£<%= tag.total %></td>
    </tr>
    <% end %>
  </table>

<p> <a href="/categories/new">Add a new Category</a> </p>
